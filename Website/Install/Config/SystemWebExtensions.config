<configuration>
	<nodes configfile="Web.config">
    <!-- Remove system.web.extensions section node if the corresponding section does not exist -->
    <node path="/configuration/configSections/sectionGroup[@name = 'system.web.extensions']" action="remove" />
  </nodes>
</configuration>
