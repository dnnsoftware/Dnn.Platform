(function () {

    // Get module.
    var module = angular.module('cantarus.poly-deploy');

    // Create controller.
    module.controller('UploadController', ['$scope', 'SessionService',
        function ($scope, SessionService) {

            

        }]);

})();
