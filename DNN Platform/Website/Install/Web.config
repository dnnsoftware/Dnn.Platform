<?xml version="1.0"?>
<configuration>
  <system.web>
  </system.web>
  <system.webServer>
    <staticContent>
      <!-- Disable client side caching for install resources -->
      <clientCache cacheControlMode="DisableCache" />
    </staticContent>
  </system.webServer>
</configuration>
