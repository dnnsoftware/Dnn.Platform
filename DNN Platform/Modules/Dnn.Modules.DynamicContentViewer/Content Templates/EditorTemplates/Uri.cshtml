@model Uri

@{
    if (Model != null)
    {
        @Html.TextBox(ViewData["FieldName"] as string, Model.ToString())
    }
    else
    {
        @Html.TextBox(ViewData["FieldName"] as string, String.Empty)
    }
}